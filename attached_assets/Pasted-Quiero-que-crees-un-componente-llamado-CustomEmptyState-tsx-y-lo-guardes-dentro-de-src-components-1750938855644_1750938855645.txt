Quiero que crees un componente llamado `CustomEmptyState.tsx` y lo guardes dentro de `src/components/ui-custom/misc/`.

---

### 🎯 Objetivo:
Este componente representa un **estado vacío contextual** (por ejemplo, cuando no hay proyectos, tareas, etc.). Debe mostrar una caja con borde punteado y estilo visual llamativo, que puede contener:

- Un ícono personalizado (usando cualquier ícono de Lucide).
- Un título (`title`).
- Un texto descriptivo (`description`).
- Un botón opcional (`action`), que puede ser omitido si no se necesita.

---

### 💡 Props esperadas:

```ts
interface CustomEmptyStateProps {
  icon: React.ReactNode;
  title: string;
  description?: string;
  action?: React.ReactNode;
  className?: string;
}
🎨 Estilos:
El contenedor debe tener:

Un borde punteado (border-dashed) usando el color var(--accent).

Padding generoso (p-8 o similar).

Un rounded-xl o rounded-2xl.

Fondo opcional (bg-muted/10) o bg-background.

Transición suave.

Debe ocupar el 100% del ancho disponible y tener máximo 500–600px centrado (max-w-lg mx-auto).

✨ Animación visual opcional:
Agregá un efecto de partículas o puntos flotantes muy sutiles, solo si no impacta en performance.

Podés usar un simple <div className="animate-pulse bg-accent/20 rounded-full w-2 h-2 ..."> como efecto visual decorativo.

Esto debe ser muy sutil, decorativo, no funcional ni invasivo.

🧩 Comportamiento:
Si no se pasa action, simplemente no renderizar el botón.

Si se pasa, renderizarlo centrado debajo del texto.

El ícono debe estar arriba, centrado, dentro de un círculo claro con sombra suave.

🧪 Ejemplo de uso:
tsx
Copiar
Editar
<CustomEmptyState
  icon={<Folder className="w-8 h-8 text-muted-foreground" />}
  title="Aún no tienes proyectos"
  description="Comienza creando tu primer proyecto para esta organización"
  action={
    <Button onClick={handleCreateProject}>
      <Plus className="w-4 h-4 mr-2" />
      Crear Proyecto
    </Button>
  }
/>
Usá Tailwind para todos los estilos. Mantené el diseño elegante, aireado y consistente con la UI existente. Asegurate de que el componente sea completamente reutilizable para múltiples contextos (proyectos, tareas, clientes, etc).