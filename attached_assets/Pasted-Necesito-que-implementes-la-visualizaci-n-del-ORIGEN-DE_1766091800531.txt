Necesito que implementes la visualización del ORIGEN DE REGISTRO de los usuarios (acquisition / tracking) en el panel de Administración de Seencel.

CONTEXTO IMPORTANTE:
- Ya existe la tabla `user_acquisition`
- Cada fila representa el origen del usuario en su registro
- Campos relevantes:
  - source (youtube, instagram, google, direct, etc)
  - medium
  - campaign
  - content
  - landing_page
  - referrer
- La relación es: user_acquisition.user_id → users.id
- Siempre existe como mínimo 1 fila por usuario
- Si el usuario entró directo, source = 'direct'

OBJETIVO FINAL (UX):
Mostrar de forma clara y profesional de dónde vino cada usuario.

--------------------------------------------------
1️⃣ TAB: ADMINISTRACIÓN → RESUMEN
CARD: “ÚLTIMOS REGISTRADOS”
--------------------------------------------------

Actualmente cada card muestra:
- Nombre del usuario
- Nombre de la organización
- Fecha / hora

NECESITO QUE:
- Debajo del nombre de la organización se agregue UNA TERCERA LÍNEA
- Esa línea debe mostrar el ORIGEN DEL USUARIO

Formato recomendado (ejemplo):
- “Origen: YouTube · campaña launch”
- “Origen: Instagram (bio)”
- “Origen: Directo”
- “Origen: Google Ads”

Reglas de render:
- Siempre mostrar `source`
- Mostrar `medium` solo si existe
- Mostrar `campaign` solo si existe
- Si source = 'direct', mostrar simplemente “Origen: Directo”
- No mostrar valores null
- Tipografía más chica / secundaria (muted / subtle)

Backend / Data:
- Ajustar el query que trae “últimos registrados”
- Hacer LEFT JOIN con user_acquisition
- Traer los campos necesarios
- No duplicar usuarios
- No romper performance

--------------------------------------------------
2️⃣ TAB: ADMINISTRACIÓN → USUARIOS
TABLA DE USUARIOS
--------------------------------------------------

Necesito extender la tabla existente.

REQUERIMIENTOS:
- Agregar UNA NUEVA COLUMNA AL FINAL de la tabla
- Título de la columna: “Origen”
- En cada fila mostrar el origen del usuario

Opciones válidas de UI (elegir la mejor):
A) Una sola columna con texto compacto:
   - “YouTube · campaña launch”
   - “Instagram”
   - “Directo”

B) Dos columnas separadas:
   - Columna “Fuente” (source)
   - Columna “Campaña” (campaign, opcional)

Reglas:
- Nunca mostrar null
- Si no hay campaign, dejar vacío o “—”
- Mantener coherencia visual con el resto de la tabla
- Ordenable (si la tabla ya soporta sorting)
- Incluible en el buscador si aplica

--------------------------------------------------
3️⃣ CONSULTAS / ESTRUCTURA
--------------------------------------------------

- Crear (si hace falta) una VIEW optimizada tipo:
  - admin_users_with_acquisition
- O extender el query existente con JOIN
- No duplicar lógica
- No hardcodear strings
- Mantener naming consistente con el resto del proyecto

--------------------------------------------------
4️⃣ CRITERIOS DE CALIDAD (MUY IMPORTANTE)
--------------------------------------------------

- No romper nada existente
- No inventar datos
- Si un usuario no tiene fila en user_acquisition (caso raro), mostrar:
  “Origen: desconocido”
- Código limpio, legible y mantenible
- Seguir el patrón actual del proyecto (hooks, services, queries, etc)

--------------------------------------------------
RESULTADO ESPERADO:
--------------------------------------------------

✔ En RESUMEN → Últimos registrados, cada card muestra claramente de dónde vino el usuario  
✔ En USUARIOS → la tabla tiene una columna nueva con el origen  
✔ Visualmente claro, profesional y consistente  
✔ Listo para usar en análisis de crecimiento y marketing

Si algo no está claro, priorizar una solución simple y correcta antes que compleja.
