declare
  replit_url text := 'https://seencel.com/api/email/send';
  email_subject text;
  user_email text;
  user_name text;
begin
  --------------------------------------------------------------------
  -- 1) NUEVO USUARIO REGISTRADO
  --------------------------------------------------------------------
  if TG_TABLE_NAME = 'users' 
     and TG_TABLE_SCHEMA = 'auth'
     and TG_OP = 'INSERT' then

    user_email := NEW.email;
    user_name := coalesce(NEW.raw_user_meta_data->>'full_name', 'Arquitecto');

    email_subject := '¡Bienvenido a Seencel! 🏗️';

    perform extensions.http((
      'POST',
      replit_url,
      ARRAY[extensions.http_header('Content-Type', 'application/json')],
      'application/json',
      jsonb_build_object(
        'to', user_email,
        'subject', email_subject,
        'template', 'welcome',
        'userName', user_name,
        'notifyAdmin', true
      )::text
    )::extensions.http_request);

  --------------------------------------------------------------------
  -- 2) NUEVA COMPRA DE CURSO (PAGO ONLINE)
  --------------------------------------------------------------------
  elsif TG_TABLE_NAME = 'purchases'
        and TG_TABLE_SCHEMA = 'public'
        and TG_OP = 'INSERT' then

    select email into user_email 
    from auth.users 
    where id = NEW.user_id;

    email_subject := 'Confirmación de Compra: ' || NEW.course_name;

    perform extensions.http((
      'POST',
      replit_url,
      ARRAY[extensions.http_header('Content-Type', 'application/json')],
      'application/json',
      jsonb_build_object(
        'to', user_email,
        'subject', email_subject,
        'template', 'purchase',
        'userName', 'Estudiante',
        'courseName', NEW.course_name,
        'amount', '$' || NEW.amount,
        'transactionId', NEW.id::text,
        'notifyAdmin', true
      )::text
    )::extensions.http_request);

  --------------------------------------------------------------------
  -- 3) NUEVO PAGO POR TRANSFERENCIA (BANK_TRANSFER_PAYMENTS)
  --------------------------------------------------------------------
  elsif TG_TABLE_NAME = 'bank_transfer_payments'
        and TG_TABLE_SCHEMA = 'public'
        and TG_OP = 'INSERT' then

    -- buscar email del usuario que subió la transferencia
    select email into user_email
    from auth.users 
    where id = NEW.user_id;

    user_name := coalesce(NEW.payer_name, 'Cliente Seencel');

    email_subject := 'Pago por Transferencia recibido — en revisión';

    perform extensions.http((
      'POST',
      replit_url,
      ARRAY[extensions.http_header('Content-Type', 'application/json')],
      'application/json',
      jsonb_build_object(
        'to', user_email,
        'subject', email_subject,
        'template', 'bank_transfer_received',
        'userName', user_name,
        'amount', NEW.amount,
        'currency', NEW.currency,
        'payerNote', NEW.payer_note,
        'transferId', NEW.id::text,
        'status', NEW.status,
        'notifyAdmin', true
      )::text
    )::extensions.http_request);

  --------------------------------------------------------------------
  else
    return NEW;
  end if;

  return NEW;
end;
