Quiero que crees una nueva página en:

**src/pages/finances/ProjectInstallmentsPage.tsx**

Usá como base el archivo `prompts/ai-page-template.md`, y respetá la estructura de layout general de la app. También, si necesitás usar modales, usá `prompts/ai-modal-template.md` y guardalos en:

**src/modals/finances/**

---

### Objetivo

Esta nueva página debe permitir al usuario **gestionar las cuotas (aportes de dinero)** hechas por inversores/contactos dentro de un proyecto de arquitectura.

---

### Acceso

Agregá un botón en el **sidebar dentro de FINANZAS**, debajo del botón “Movimientos”, con el texto:

🧾 Aportes

---

### Estructura de la página

#### Header

- Título: “Aportes al Proyecto”
- Filtro de Proyecto actual (ya lo usamos en otras páginas).
- Botón “Agregar Aporte” que abre el modal `NewInstallmentModal.tsx`.

---

#### Tabla principal

Debe listar todos los movimientos que cumplen:

```ts
concept_id === ID_DEL_CONCEPTO_CUOTAS
Y deben estar asociados a un proyecto determinado.

Mostrá estas columnas:

Fecha

Contacto (de contact_id)

Monto

Moneda

Billetera

Estado (si tiene, opcional)

Botón de acciones (ver/editar/eliminar)

Modal de creación (NewInstallmentModal.tsx)
Guardalo en:

src/modals/finances/NewInstallmentModal.tsx

Usá el template ai-modal-template.md.

Este modal debe permitir registrar un nuevo aporte/cuota con los siguientes campos:

Fecha

Monto

Moneda

Billetera

Contacto (relacionado al proyecto actual → filtrar por project_investors)

Número de cuota (opcional)

Adjuntar archivo (opcional)

Notas (opcional)

Este modal debe crear un movimiento en la tabla movements con:

concept_id = CUOTAS

contact_id = contacto seleccionado

Extras
En el listado, mostrá el total aportado al proyecto (sumatoria de cuotas).

Si no hay cuotas cargadas, mostrale al usuario un mensaje: “Aún no hay aportes registrados para este proyecto”.

Sobre datos
Las relaciones necesarias ya existen en Supabase:

La tabla movements tiene contact_id (opcional).

El concepto “Cuotas” ya existe en movement_concepts.

Los contactos del proyecto están en project_investors.

UX esperada
Esta página debe servir como historial filtrado de todos los aportes que hicieron las personas vinculadas a un proyecto. Cada cuota es un movimiento real de ingreso.

La misma acción de crear una cuota debe poder hacerse desde esta página o desde el modal general de movimientos con una opción adicional (se desarrollará luego).