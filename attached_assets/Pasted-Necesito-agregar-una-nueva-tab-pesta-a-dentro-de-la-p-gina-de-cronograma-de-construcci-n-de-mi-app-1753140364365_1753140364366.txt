Necesito agregar una nueva tab (pestaña) dentro de la página de cronograma de construcción de mi app. Esta pestaña se debe llamar **"Análisis Visual"** y debe contener 9 gráficos relacionados al Gantt. Están organizados en un layout de 3 filas por 3 columnas (grid de 3x3), todos dentro de un `<TabsContent value="analytics">...</TabsContent>` o el componente que estés usando para manejar tabs.

### 🔁 Datos
Todos los gráficos deben obtener sus datos desde las tareas de construcción (`construction_tasks`) que ya están cargadas en la página y disponibles en `data` o `allTasks`, con los campos necesarios como: `start_date`, `end_date`, `duration_in_days`, `progress_percent`, `rubro_name`, `category_name`, `phase_name`, etc.

### 📁 Organización de archivos
Cada gráfico debe estar en su propio archivo dentro de esta carpeta:
src/components/charts/gantt/

yaml
Copiar
Editar
Por ejemplo:
ProgressCurve.tsx

BurndownChart.tsx

WorkloadOverTime.tsx

TasksByPhase.tsx

DurationByRubro.tsx

StatusBreakdown.tsx

CriticalPathDistribution.tsx

WeeklyProgressHeatmap.tsx

DependencyNetwork.tsx

markdown
Copiar
Editar

### 📊 Gráficos que quiero (iniciales)
1. **Curva de Avance Físico** (ProgressCurve): línea acumulada de `progress_percent` vs fecha
2. **Burndown Chart** (BurndownChart): tareas pendientes vs tiempo
3. **Carga de Trabajo Diaria** (WorkloadOverTime): cuántas tareas activas por día
4. **Tareas por Fase** (TasksByPhase): torta o barras apiladas por `phase_name`
5. **Duración Promedio por Rubro** (DurationByRubro): barra vertical
6. **Estado de Tareas** (StatusBreakdown): completadas / en progreso / no iniciadas
7. **Distribución de Tareas Críticas** (CriticalPathDistribution): barras de tareas críticas vs no críticas
8. **Heatmap de Avance Semanal** (WeeklyProgressHeatmap): visualiza % por semana
9. **Red de Dependencias** (DependencyNetwork): tipo force-graph (si no se puede, una tabla con info de dependencias también sirve)

### 🧱 Layout
Usá un grid de Tailwind como este:
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
  {/* Acá van los 9 gráficos */}
</div>
📦 Librerías
Usá recharts o nivo para los gráficos. Si no está instalada, asumí que recharts ya está disponible.

🧪 Asegurate de:
Todos los componentes deben exportar una función default

Cada componente debe aceptar props genéricas como data

Deben poder mostrarse sin errores aunque haya pocos datos

Mostrame algo en pantalla aunque sea con mockData para ver la estructura

Agregá esta nueva tab dentro de la página de cronograma de construcción (src/pages/construction/schedule/CronogramaPage.tsx o el archivo correspondiente), como una nueva entrada en el sistema de tabs existentes.