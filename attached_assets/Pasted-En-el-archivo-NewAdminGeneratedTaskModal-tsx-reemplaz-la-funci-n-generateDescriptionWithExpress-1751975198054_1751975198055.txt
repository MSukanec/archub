En el archivo `NewAdminGeneratedTaskModal.tsx`, reemplazá la función `generateDescriptionWithExpressions()` por una nueva versión que **construya la frase desde cero**, utilizando el campo `expression_template` de cada parámetro.

Ya no debe usar `template.name_template`, sino hacer lo siguiente:

---

### 🔧 Nueva lógica:

1. Obtener el nombre de la plantilla (`template.name`)
2. Iterar sobre los parámetros (`parameters`), ordenados por `position`
3. Para cada parámetro:
   - Obtener el valor ingresado por el usuario (`paramValues[param.name]`)
   - Si es `select`, buscar la opción y tomar el `label`
   - Aplicar el `expression_template` del parámetro (por defecto usar `"{value}"`)
   - Reemplazar `{value}` por el label o valor del input
4. Concatenar los fragmentos generados
5. Preceder la frase con el nombre de la plantilla y terminar con `"."`

---

### 🧩 Código sugerido:

```ts
const generateDescriptionWithExpressions = (paramValues: Record<string, any>) => {
  if (!parameters) return "Seleccione los parámetros para ver la vista previa";

  const currentTemplate = templates?.find(t => t.id === selectedTemplateId);
  if (!currentTemplate) return "Seleccione una plantilla";

  let fragments: string[] = [];

  parameters
    .sort((a, b) => a.position - b.position)
    .forEach(param => {
      const rawValue = paramValues[param.name];
      if (!rawValue) return;

      // Buscar label para select
      let label = rawValue.toString();
      const option = parameterOptions[param.id]?.find(opt => opt.value === rawValue);
      if (option?.label) label = option.label;

      // Aplicar plantilla del parámetro
      const expr = param.expression_template || '{value}';
      const fragment = expr.replace('{value}', label);

      fragments.push(fragment);
    });

  return `${currentTemplate.name} ${fragments.join(' ')}.`.trim();
};
Reemplazá la versión actual de generateDescriptionWithExpressions por esta, y asegurate de que tanto en la vista previa como en el nombre guardado de la tarea se use esta función.

Esto hará que la descripción se vea correctamente, por ejemplo:

Muros Simples de Ladrillo Hueco con mortero de cal de espesor 15 cm.