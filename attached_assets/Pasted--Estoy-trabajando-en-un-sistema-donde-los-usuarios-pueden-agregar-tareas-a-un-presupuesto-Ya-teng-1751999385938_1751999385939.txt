⚠️ Estoy trabajando en un sistema donde los usuarios pueden agregar tareas a un presupuesto. Ya tengo un modal (Agregar Tarea al Presupuesto) que permite seleccionar una tarea existente desde task_generated, buscando por nombre. Pero ahora necesito mejorar la experiencia para usuarios comunes (no administradores), permitiéndoles crear nuevas tareas si no encuentran una adecuada.

🎯 Objetivo:
Si el usuario no encuentra la tarea buscada, debe poder crear una nueva tarea de forma guiada y sencilla, sin salir del flujo actual.

✅ Modificaciones necesarias:
1. En el TaskSearchComboBox actual:
Cuando no haya resultados, mostrar debajo del input un mensaje como:

yaml
Copiar
Editar
❗ No se encontraron tareas. Puede crear una nueva tarea personalizada.
Agregar un botón: + Crear Tarea Personalizada

Este botón debe abrir un nuevo modal llamado CreateGeneratedTaskUserModal.tsx

2. Crear nuevo modal CreateGeneratedTaskUserModal.tsx
Este modal debe permitir al usuario común crear una nueva tarea personalizada a partir de una plantilla:

Campos requeridos:

template_id: selector de plantilla (task_templates)

param_values: dinámico, depende de la plantilla seleccionada (usar la misma lógica que el modal de admin)

unit_id: selector de unidad (units)

Valores ocultos o predefinidos:

organization_id: tomar del contexto del usuario actual

is_system: false (no mostrar ni permitir cambiar)

Al guardar:

Llamar a la función task_generate_code(...) con los parámetros necesarios.

Si la tarea ya existe, traerla.

Si no existe, crearla y devolverla.

Al finalizar:

Cerrar el modal.

Insertar esa nueva tarea automáticamente en el campo del TaskSearchComboBox original y continuar con el flujo de "Agregar tarea al presupuesto".

⚠️ Consideraciones:
La función SQL ya está preparada y acepta:
(input_template_id, input_param_values, input_organization_id, input_unit_id, input_is_system)

Asegurate de que el modal use la misma estética y tamaño que los modales de usuario, no los de administración.

El usuario no debe poder seleccionar si la tarea es del sistema o no.

Todo debe quedar guardado correctamente en task_generated, incluyendo unit_id y organization_id.