EL PROMPT PARA REPLIT (Modo Heatmap)
ROLE: Senior Frontend Engineer & Data Visualization Specialist.

OBJECTIVE: Create a second POC for the futuristic financial dashboard, focusing on the "Heatmap" view. This will live on a new, isolated route.

THE VISION ("The Financial Heatmap"): A structured, grid-based visualization of the client portfolio.

Each cell represents a client/unit.

The color of the cell indicates their financial health (Red=Critical, Yellow=Warning, Green=Healthy).

The goal is to give a quick, bird's-eye view of the portfolio's health and identify clusters of debt.

The interaction should be clean and informative (hover for details).

TECHNICAL STACK & REQUIREMENTS:

Framework: Vite + React + TypeScript.

Styling: Tailwind CSS (Crucial for the grid and color mapping).

Animation: framer-motion (for smooth hover effects and future transitions).

Data: Use the EXACT SAME MOCK DATA STRUCTURE as the previous "Network" POC (an array of 50-100 clients with status, debtAmount, etc.). This is vital for future integration.

STEP-BY-STEP INSTRUCTIONS:

1. Create the Isolated Route: Create a new page component located at src/pages/lab/FinancialHeatmapPage.tsx. Add a route in the main router (e.g., App.tsx) so I can access it via /lab/financial-heatmap.

2. Define Mock Data (in FinancialHeatmapPage.tsx): Copy the mock data array from the Network POC to ensure consistency.

3. Implement the Heatmap Visualization:

Use a CSS Grid container (display: grid) to arrange the clients.

The grid should be responsive, showing more columns on wider screens.

Each client should be rendered as a "Card" or "Cell" within the grid.

4. Styling & Color Mapping (Tailwind):

Map the client's status to Tailwind background colors:

status: 'critical' -> bg-red-600 (vibrant red)

status: 'warning' -> bg-yellow-500 (vibrant yellow)

status: 'healthy' -> bg-green-500 (vibrant green)

The cells should look modern and clean (rounded corners, slight shadow, clean typography for the unit number/name).

5. Implement Hover Interactions (Framer Motion):

When a user hovers over a cell:

The cell should slightly scale up (scale: 1.05) using Framer Motion for smoothness.

A tooltip or overlay should appear showing more details (Exact debt amount, last interaction date).

The non-hovered cells could slightly dim to emphasize the focused one.

DELIVERABLE: I want to navigate to /lab/financial-heatmap and see a beautiful, colored grid of client cells. Hovering over red cells should feel urgent and reveal debt details.