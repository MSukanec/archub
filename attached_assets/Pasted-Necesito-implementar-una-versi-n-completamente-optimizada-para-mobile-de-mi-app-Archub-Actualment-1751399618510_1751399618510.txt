Necesito implementar una versión completamente optimizada para mobile de mi app (Archub). Actualmente tengo un sidebar de navegación y un header con breadcrumb y selectores de organización/proyecto. En mobile necesito una **versión distinta y simplificada**.

📱 MOBILE MENU (fullscreen)

1. Cuando el viewport sea menor a `md`, al hacer clic en el botón hamburguesa en el header, debe abrirse un **menú lateral tipo overlay fullscreen**, que ocupe el 100% de la pantalla (`fixed inset-0 z-50`).

2. Este menú debe:
   - Mostrar exactamente los **mismos botones y estructura** que el `Sidebar.tsx`, incluyendo:
     - Los mismos grupos y subgrupos (usando acordeones si es necesario).
     - Mantener los íconos y textos como en desktop.
   - Ser completamente scrollable en vertical (`overflow-y-auto`).

3. Abajo del menú, en posición fija (`fixed bottom-0 w-full`), debe haber un **footer horizontal (zócalo)** con los 3 botones que normalmente están abajo del sidebar:
   - Administración
   - Tareas
   - Perfil
   - Cada uno con su ícono, en línea como la referencia que muestro (botones inline tipo iPhone app).

4. Encima del listado de botones, debe haber:
   - Un **select para ORGANIZACIÓN**
   - Un **select para PROYECTO**
   - Ambos con diseño comprimido tipo dropdown (tailwind `select` o personalizado)

📍 Ejemplo de estructura visual esperada:

< [Cerrar]
[Select: Organización]
[Select: Proyecto]
-------------------------------------
> Resumen del Proyecto
- Cronograma
- Planificación
> Finanzas
- Resumen
- Movimientos
-------------------------------------
🔧 Administración 🧱 Tareas 👤 Perfil

markdown
Copiar
Editar

📤 HEADER EN MOBILE

1. En mobile, el header debe ser más simple:
   - No mostrar breadcrumbs ni rutas largas.
   - Solo mostrar el **nombre de la vista actual** (ej. “Resumen de Finanzas”) centrado.
   - A la derecha, incluir el **ícono hamburguesa** (☰) que abre el menú mobile.

2. Debe mantener:
   - Altura reducida (ej. `h-14`)
   - Color de fondo igual al header actual.
   - Padding y fuente adaptados a móviles.

📦 ESTRUCTURA DE ARCHIVOS

- `MobileMenu.tsx`: nuevo componente para el menú mobile.
- `Header.tsx`: adaptado para detectar si es mobile (con Tailwind `hidden md:flex` y `flex md:hidden`).
- `useMobileMenuStore.ts`: si necesitás manejar open/close como estado global.

📱 RESPONSIVE DESIGN

- Todo debe activarse solo en `md:hidden` y mantenerse desactivado en desktop.
- Asegurate de no duplicar lógica, sino reutilizar el árbol de navegación del sidebar actual.

🧠 OPCIONAL SI TENÉS TIEMPO

- Agregar una animación para que el mobile menu entre deslizando desde la izquierda.
- Recordar la última organización/proyecto seleccionados en localStorage.

Esto te da una experiencia mobile moderna tipo app nativa, como Notion o Stripe Mobile, sin perder coherencia con la experiencia de escritorio.