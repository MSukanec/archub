Contexto general (leer completo antes de actuar):

Estoy desarrollando una EXTENSIÓN DE CHROME externa llamada “Guardar en Seencel”.
La extensión NO vive en este proyecto y NO la conocés.
La extensión funciona como un cliente externo que consume endpoints HTTP del backend.

Actualmente existe un backend con Supabase (Auth + DB + Storage) y un modelo de datos ya definido y en uso.

El problema actual NO es la base de datos ni la extensión:
FALTA una capa de CONTEXTO que permita a la extensión saber:
- quién es el usuario
- cuál es su organización activa
- qué proyectos tiene
- qué tablero de inspiración usar por proyecto

Tu tarea es IMPLEMENTAR COMPLETAMENTE el soporte backend para la extensión, SIN hardcodear nada y SIN romper flujos existentes.

---

## 1️⃣ OBJETIVO PRINCIPAL

Crear la infraestructura backend necesaria para que una extensión externa pueda:

1. Obtener el contexto real del usuario (org / proyectos / tableros)
2. Guardar un pin REAL en el tablero correcto
3. Eliminar cualquier dependencia de strings hardcodeados como “Seencel”

---

## 2️⃣ MODELO DE DATOS (YA EXISTE – NO CAMBIAR)

Asumí que estas tablas ya existen y son correctas:

- organizations
- projects
- pin_boards
- pins
- pin_board_items
- media_files
- media_links
- user_preferences (con last_organization_id)

Relación real esperada:
organization → project → pin_board → pin_board_items → pins

NO crear nuevas tablas salvo que sea estrictamente necesario.
NO modificar el esquema existente sin justificarlo explícitamente.

---

## 3️⃣ ENDPOINT A CREAR (OBLIGATORIO)

### GET /api/extension/context

Este endpoint debe:

- Requerir autenticación JWT:
  Authorization: Bearer <supabase_access_token>

- Validar el token con Supabase Auth
- Resolver el usuario interno (users.auth_id)
- Obtener la organización activa desde user_preferences.last_organization_id
- Verificar que el usuario sea miembro de esa organización
- Listar los proyectos de esa organización
- Para cada proyecto:
  - buscar un pin_board por defecto (por ejemplo name = 'Inspiración')
  - si NO existe, crearlo automáticamente
- Retornar un JSON con este formato EXACTO:

{
  "user": {
    "id": "uuid",
    "email": "email"
  },
  "organization": {
    "id": "uuid",
    "name": "string"
  },
  "projects": [
    {
      "id": "uuid",
      "name": "string",
      "default_board": {
        "id": "uuid",
        "name": "string"
      }
    }
  ]
}

NO devolver datos innecesarios.
NO hardcodear nombres.
NO asumir un único proyecto.

---

## 4️⃣ ENDPOINT EXISTENTE A AJUSTAR (SI HACE FALTA)

### POST /api/pins/save

Verificar que este endpoint:

- Acepte:
{
  "title": "...",
  "source_url": "...",
  "image_url": "...",
  "project_id": "uuid",
  "board_id": "uuid"
}

- Valide:
  - autenticación JWT
  - que project_id pertenezca a la organización activa
  - que board_id pertenezca a ese proyecto

- Ejecute correctamente:
  - descarga de imagen (con protección SSRF)
  - subida a Supabase Storage
  - creación de media_files
  - creación de pins
  - creación de media_links
  - creación de pin_board_items

- Devuelva:
{
  "ok": true,
  "pin": { ... },
  "board_id": "uuid"
}

Si algo de esto no está implementado correctamente, corregilo.

---

## 5️⃣ SEGURIDAD Y CALIDAD

- Usar Supabase Auth correctamente
- No confiar en input del cliente
- Manejar errores explícitos (401, 403, 400, 500)
- CORS debe permitir requests desde extensiones Chrome
- El endpoint /api/extension/context NO debe modificar datos salvo crear el board default si no existe

---

## 6️⃣ QUÉ NO HACER

- NO tocar la extensión
- NO hardcodear nombres como “Seencel”
- NO asumir que hay un único proyecto
- NO crear UI
- NO inventar nuevas features
- NO romper compatibilidad con el frontend existente

---

## 7️⃣ ENTREGABLES

Quiero que devuelvas:

1. Código completo del endpoint GET /api/extension/context
2. Cualquier ajuste necesario a POST /api/pins/save
3. Explicación breve del flujo final (extensión → backend → DB)
4. Lista de supuestos que estás haciendo (si los hay)

El código debe ser listo para copiar y pegar.
No asumas nada que no esté explícito en este prompt.
