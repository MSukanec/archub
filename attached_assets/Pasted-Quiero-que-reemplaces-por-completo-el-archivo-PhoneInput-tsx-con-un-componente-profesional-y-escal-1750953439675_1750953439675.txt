Quiero que reemplaces por completo el archivo `PhoneInput.tsx` con un componente profesional y escalable.

Debe hacer lo siguiente:

---

### 📱 Componente: `PhoneInput`

Debe ser un campo de teléfono con selección de país. Lo quiero con:

- Un botón a la izquierda para seleccionar el país.
- A la derecha, el campo de texto para ingresar el número de teléfono.

---

### ✅ Diseño del selector de país:

- Mostrar la bandera real del país (usa emojis o unicode, no solo las siglas "AR", "US", etc.).
- A la derecha de la bandera, mostrar solo el código de discado internacional (ej: `+54`, `+1`, `+52`).
- Eliminar completamente las siglas (no mostrar más "AR AR").
- Que se vea prolijo, alineado y bien espaciado.
- Fondo: `var(--input-bg)`
- Texto: `var(--input-fg)`
- Borde: `1px solid var(--input-border)`
- Sin efecto hover (que al pasar el mouse no cambie color).
- Transición `none` para evitar cambios suaves.
- Padding interno de `px-3 py-2` y alto `h-10`.
- Ancho mínimo del botón: `75px`.

---

### 🧭 Dropdown de selección de país:

- Aparece un `Popover` al hacer clic en el botón de país.
- Mostrar en cada ítem: bandera + nombre del país + código de discado.
- Ejemplo: 🇦🇷 Argentina +54
- Cuando está seleccionado un país, que se destaque con:
  - Fondo: `var(--accent-bg)`
  - Texto: `var(--accent-fg)`
- Borde del dropdown: `var(--input-border)`
- Quitar cualquier sombra de hover. Que se vea limpio.

---

### 📦 Datos:

- Puede ser una lista interna de países con:
  - `code`: AR, US, etc.
  - `name`: Argentina, Estados Unidos, etc.
  - `dialCode`: +54, +1, etc.
  - `flag`: usar emojis (🇦🇷, 🇺🇸, 🇲🇽, etc).

---

### ✍ Input de teléfono:

- A la derecha del botón, va un input con placeholder `"Número de teléfono"`.
- Bordes y altura deben alinearse perfectamente con el botón izquierdo.
- Borde izquierdo debe unirse visualmente al botón (sin doble borde).

---

### 🧩 Props:

El componente debe aceptar:

```tsx
interface PhoneInputProps {
  value: string;
  onChange: (value: string) => void;
}
Cuando el país cambia, que no se cambie el número de teléfono, solo afecta el dialCode que se muestra a la izquierda.

🧪 Bonus (opcional):
Si querés hacerlo más completo, podés agregar validación básica para evitar caracteres no numéricos, pero no es obligatorio.

Ubicá este componente en:

swift
Copiar
Editar
src/components/ui-custom/forms/PhoneInput.tsx
Sobrescribí por completo el archivo actual.

El resultado debe verse limpio, moderno, y coherente con el resto del diseño del sistema (como los formularios de Archub).

yaml
Copiar
Editar
