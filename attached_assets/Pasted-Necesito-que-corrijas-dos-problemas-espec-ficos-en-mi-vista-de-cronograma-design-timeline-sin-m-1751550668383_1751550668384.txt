Necesito que corrijas dos problemas específicos en mi vista de cronograma (`/design/timeline`) sin modificar lo que ya funciona bien:

---

### 🔧 1. El timeline no tiene scroll horizontal y está completamente estático

🚫 PROBLEMA:
- Antes el timeline tenía scroll horizontal correcto (solo en la parte de fechas y barras).
- Ahora desapareció y no hay forma de desplazarse por los días.
- Además, el scroll parece estar metido **dentro de la última fase**, en lugar de estar al final del timeline general.

✅ SOLUCIÓN:
- El `scroll-x` debe estar aplicado a un contenedor que abarque solamente el **timeline derecho**, excluyendo la columna izquierda de texto.
- Asegurate de que la estructura sea:

```tsx
<div className="flex">
  <div className="sticky left-0 z-10 w-[260px] bg-white">
    {/* Lista de fases y tareas */}
  </div>
  <div className="overflow-x-auto min-w-fit">
    {/* GanttGrid con columnas y barras */}
  </div>
</div>
El overflow-x-auto debe estar fuera de cualquier GanttRow o Phase, para que el scroll horizontal esté al final del timeline, no adentro de una card o fila.

Debajo del timeline debe haber espacio visual (pb-16 o similar) para que el scroll no se vea encajonado.

Al final de la lista debe haber un botón:

tsx
Copiar
Editar
<button className="mt-6 text-sm text-muted-foreground hover:underline">+ Crear nueva fase</button>
🔧 2. Los botones "Desde", "Hasta" y "Ir a HOY" no hacen nada
🚫 PROBLEMA:

Los campos de fecha (Desde, Hasta) no modifican el rango del timeline.

El botón “Ir a HOY” no scrollea hasta la columna del día actual ni lo resalta correctamente.

✅ SOLUCIÓN:

Los campos Desde y Hasta deben actualizar el estado global de fechas (timelineRange.start y timelineRange.end) en useGanttStore.

Esto debería provocar un refetch o recalcular getDateArray() con el nuevo rango.

El botón “Ir a HOY” debe hacer dos cosas:

Visualmente: aplicar un scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' }) sobre el día actual dentro del timeline.

Lógicamente: resaltar el día actual con un fondo (bg-blue-100) o línea azul como ya se hace.

🧠 ACLARACIÓN:

No toques el sistema de vista (día / semana / mes) ni la forma en que se muestran las fases y tareas.

Solo corregí lo visual y funcional de estos dos puntos.