🔵 PROMPT PARA REPLIT — PEGAR TAL CUAL

Necesito que reconstruyas por completo el sistema de Modals de mi proyecto.

Tu objetivo es eliminar código obsoleto, simplificar la arquitectura, unificar patrones y dejar todo funcionando a nivel SaaS premium (Linear, Vercel, Notion).

Aquí están las instrucciones completas, paso a paso.
Seguí todas sin omitir nada.

🟦 🧩 0. CONTEXTO (LEER ANTES DE CODIFICAR)

Ya tengo:

ModalLayout.tsx (desktop modal)

DrawerBase.tsx (mobile sheet)

ModalHeader, ModalBody, ModalFooter, ModalStepHeader, ModalStepFooter, ModalSectionButton (estos SE MANTIENEN)

globalModalStore.ts (pero hace falta refactor)

registry.ts y registerModals.ts (hay que refactorizar)

una lista enorme de tipos ModalType

un panelStore.ts que YA NO debe existir

ModalFactory.tsx que YA NO debe existir

Quiero que reorganices, elimines y reescribas las partes necesarias para dejar un sistema perfecto, moderno y simple.

🟦 1. ELIMINAR ARCHIVOS OBSOLETOS

Eliminar por completo:

✔ ModalFactory.tsx
✔ panelStore.ts
✔ modal-best-practices.tsx (si existe)
✔ cualquier documentación interna duplicada en utils/

No deben volver a utilizarse.

🟦 2. REFACTORIZAR globalModalStore.ts

Reescribilo completamente con esta estructura exacta:

✔ Mantener solo el stack como fuente de verdad.

Eliminar:

open

type

data

✔ Mantener estas funciones:

openModal(type, data?) → reemplaza toda la pila por un solo modal.

pushModal(type, data?) → agrega un modal arriba.

popModal() → saca el modal superior.

closeAll()

updateModalData(data) → actualiza solo el último modal.

✔ Agregar:

canCloseModal: boolean según flags del modal actual (del registry)

setCanCloseModal()

blockCloseForDirtyForms: boolean

setBlockClose() / clearBlockClose()

✔ El store debe exponer:

currentModal (top del stack)

isOpen = stack.length > 0

🟦 3. CREAR ModalProvider.tsx → EL CORAZÓN DEL SISTEMA

Crear un archivo nuevo en:

src/components/modal/ModalProvider.tsx


Debe hacer lo siguiente:

✔ 3.1. Escuchar useGlobalModalStore

Renderizar todo el stack, así:

{stack.map((modal, index) => {
   const Entry = registry[modal.type];
   const z = 50 + index * 10;
   return (
     <ModalContainer 
       key={modal.id}
       entry={Entry}
       modal={modal}
       zIndex={z}
     />
   );
})}

✔ 3.2. Detectar mobile/desktop

Si isMobile:

renderizar con DrawerBase

Si desktop:

usar ModalLayout

✔ 3.3. Manejar cierre:

cierre con “ESC”

cierre con click en backdrop

NO cerrar si:

modalConfig.preventCloseOnEsc

modalConfig.preventCloseOnBackdrop

store.blockCloseForDirtyForms

✔ 3.4. Manejar “dirty forms”

Si un modal registra dirty: true → bloquear pop/cierre.

🟦 4. REFACTORIZAR registry.ts Y REGISTRO DE MODALS
✔ Debe permitir registrar metadata por modal:
registerModal("client-payment", ClientPaymentModal, {
   category: "finance",
   size: "lg",
   drawerOnMobile: true,
   preventCloseOnBackdrop: true,
   preventCloseOnEsc: false,
   mapDataToProps: (data, context) => ({
      paymentId: data?.paymentId,
      projectId: context?.projectId,
   }),
});

✔ registerModals.ts debe dividirse en archivos:
modals/
  organization/
  project/
  finance/
  learning/
  general/


Cada uno exporta su registerXYZModals().

El archivo raíz:

initializeAllModals()


hace:

registerOrganizationModals();
registerProjectModals();
registerFinanceModals();
...

✔ ModalType debe generarse así:

Eliminar lista gigante.

Nuevo tipo:

export type ModalType = keyof typeof modalRegistry;

🟦 5. MODALCONTAINER → NUEVO COMPONENTE

Crear:

src/components/modal/ModalContainer.tsx


Hace:

✔ Toma:

entry (del registry)

modal (del store)

zIndex

✔ Aplica flags del registry:

size

drawerOnMobile

headerConfig

footerConfig

✔ Llama al modal real:
<Entry.component 
    modalData={modal.data}
    onClose={popModal}
    {...mappedPropsFromRegistry}
/>

✔ Si el modal necesita “step wizard”:

El modal simplemente usa ModalStepHeader + ModalStepFooter.

🟦 6. REFACTORIZAR ModalLayout PARA QUE SOPORTE STACK

Debe recibir un prop zIndex

Debe recibir preventCloseOnBackdrop

Debe recibir preventCloseOnEsc

Debe bloquear scroll global solo cuando stack.length > 0

Debe manejar accesibilidad (focus trap, aria-label, aria-describedby)

El overlay debe oscurecer un poco más por cada modal:

Ejemplo:

opacity: 0.5 + index * 0.1

🟦 7. ACTUALIZAR DrawerBase

Alinear su API con ModalLayout:

Props obligatorios:

onClose
children
headerContent
footerContent
zIndex
preventCloseOnBackdrop

🟦 8. MANTENER ESTOS ARCHIVOS TAL CUAL

(Usarlos sin tocar la UI, solo integrarlos al nuevo sistemita):

ModalHeader.tsx

ModalBody.tsx

ModalFooter.tsx

ModalStepHeader.tsx

ModalStepFooter.tsx

ModalSectionButton.tsx

Están perfectos.

🟦 9. ELIMINAR PANELSTORE

Cada modal manejará paneles internos con useState.

🟦 10. TESTEAR

Replit debe revisar:

Todos los imports rotos después de refactor.

Que ModalProvider esté importado en App.tsx y envuelva TODA la app.

Que openModal("xxx") y pushModal("xxx") funcionen.

Que el stack funcione.

Que Drawer y ModalLayout respondan al tamaño de pantalla.

🟦 IMPORTANTE PARA REPLIT

Antes de cambiar nada → inspeccionar los siguientes archivos para reescribirlos usando los criterios anteriores:

src/components/modal/foundation/ModalLayout.tsx

src/components/modal/foundation/DrawerBase.tsx

src/components/modal/state/globalModalStore.ts

src/components/modal/registry.ts

src/components/modal/registerModals.ts

src/components/modal/factory (ELIMINAR)

src/components/modal/state/panelStore.ts (ELIMINAR)

🟦 OBJETIVO FINAL

Dejar un sistema de modales:

✔ 100% global
✔ basado en stack real
✔ tipo-safe
✔ sin factory
✔ con provider
✔ mobile/desktop unificados
✔ con metadata por modal
✔ con dirty state
✔ con control de cierre
✔ sin código duplicado
✔ sin stores innecesarios
✔ ordenado en namespaces
✔ escalable para IA / importador masivo / wizards
🟦 LISTO. EJECUTÁ TODO LO ANTERIOR.
🧨 Fin del prompt para Replit.