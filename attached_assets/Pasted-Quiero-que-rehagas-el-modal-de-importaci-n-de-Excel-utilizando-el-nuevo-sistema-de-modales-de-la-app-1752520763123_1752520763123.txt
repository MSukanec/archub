Quiero que rehagas el modal de importación de Excel utilizando el nuevo sistema de modales de la app.

🧩 Estructura actual de modales:
- Los nuevos modales usan un archivo `FormModalBody.tsx` como wrapper base.
- Los paneles internos pueden ser de tipo `form`, `view`, `edit` o `subform`.
- Queremos agregar ahora un nuevo tipo de modal basado en pasos (wizard), que tenga múltiples pantallas dentro del mismo `FormModalBody`.

🎯 Objetivo:
Reemplazar el modal antiguo de "Importar desde Excel" por uno nuevo, que use 3 pasos dentro de un solo modal y sea 100% compatible con el sistema actual. No debe afectar ningún otro modal.

🧱 Estructura del nuevo modal:
- Crear archivo: `src/modals/finances/ImportExcelModal.tsx`
- Dentro de ese archivo, manejar 3 pasos con `useState(step)`:
  - **Paso 1: Subir archivo**
    - Input de tipo archivo (CSV, XLS, XLSX)
    - Validación básica de tipo y tamaño
  - **Paso 2: Vista previa**
    - Mostrar tabla con las filas parseadas del archivo
    - Botón para avanzar o volver
  - **Paso 3: Confirmar importación**
    - Mostrar listado de filas con checks (como el modal anterior)
    - Botón para importar y spinner si está cargando

🔁 Lógica:
- Cada paso se renderiza condicionalmente según el estado `step`
- Usar `FormModalBody` como contenedor de todo
- Al importar, llamar a la función existente que parsea e inserta los movimientos
- Si hay error de formato en la fecha, mostrar feedback (como en modal anterior)
- El botón de cierre debe funcionar como siempre (icono de cruz arriba)

🎨 Estética:
- El diseño debe seguir los estilos del resto de los modales nuevos (padding, tipografía, botones, etc.)
- Cada paso debe mostrar el número y título arriba (por ejemplo: “Paso 2 de 3 – Revisa los datos”)

🧠 Extra:
- Si ya existe código previo de lógica de importación, simplemente moverlo al paso 3 del nuevo modal
- No tocar ni sobrescribir otros archivos de modales existentes
- No modificar `modalStore`, ya que este modal se abre con `modalStore.open("import-excel")`

📦 Guardar todo en: `src/modals/finances/ImportExcelModal.tsx`
