## Objetivo

Crear un componente visual reutilizable llamado `TemplateNameBuilder` que reemplace el campo de texto tradicional de “Plantilla de Nombre” por un sistema visual donde se puedan insertar parámetros dinámicos (`{{parameter}}`) seleccionándolos desde una lista.

Este componente se utilizará dentro del modal `EditTaskTemplateModal.tsx` o `TaskTemplateModal.tsx`.

---

## Requisitos funcionales

- El componente debe permitir construir una frase con:
  - partes de **texto libre**
  - partes que representen **parámetros dinámicos** (tomados de la tabla `task_template_parameters`)
- Debe renderizar los parámetros visualmente como chips, pill buttons o tags con el `label` correspondiente (ej: `Material`, `Espesor`, etc.).
- Internamente, el resultado final debe guardarse como una string con `{{name}}` (ej: `Muro de {{material}} con {{mortar_type}}...`).

---

## Detalles técnicos

### 🧾 Datos

- La tabla `task_template_parameters` ya existe.
- Cada template (`task_templates`) tiene un conjunto de parámetros relacionados (por `template_id`) ya cargados.
- Campos relevantes: `name`, `label`, `type`, `position`, etc.

### 📤 Output final

El componente debe emitir un string `value` como:
```ts
"Muro de {{material}} con {{mortar_type}}, espesor {{thickness}} cm en {{location}}"
🎯 UI deseada
Un área visual (como un editor de texto simplificado con chips).

Un botón “+ Insertar parámetro” que abre un popover o dropdown con los parámetros disponibles para ese template_id.

Posibilidad de insertar texto libre entre parámetros.

Chips editables o eliminables.

Soporte para reordenar si es fácil (drag and drop opcional).

📦 Props sugeridas para TemplateNameBuilder
tsx
Copiar
Editar
interface TemplateNameBuilderProps {
  value: string; // string actual con {{parametros}} y texto
  onChange: (newValue: string) => void;
  parameters: TaskTemplateParameter[]; // lista desde Supabase para ese template
  disabled?: boolean;
}
✅ Resultado esperado
El usuario puede construir la plantilla de nombre visualmente y el sistema genera internamente el string correcto con {{param}}. No necesita escribir {{thickness}} manualmente.

Extras si da el tiempo
Validación visual (ej: advertir si se repite un parámetro).

Tooltips sobre los chips mostrando type, unidad, etc.

Tooltip flotante que muestra cómo quedará la frase con valores de ejemplo.