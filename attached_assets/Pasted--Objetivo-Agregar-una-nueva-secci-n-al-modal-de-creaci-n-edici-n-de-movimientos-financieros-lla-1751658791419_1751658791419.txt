## Objetivo

Agregar una nueva sección al modal de creación/edición de movimientos financieros llamada **"Archivos"**, igual a cómo funciona la sección “Fotos y Videos” en la bitácora.

---

## Estructura esperada

Agregar un **acordeón nuevo** debajo de "Información Básica", con el título:

```txt
📎 Archivos (0)
(El número debe actualizarse según la cantidad de archivos cargados dinámicamente.)

Funcionalidad requerida
1. Botón: “+ Agregar Archivo”
Este botón debe permitir al usuario agregar una nueva fila de carga con los siguientes campos:

Nombre del archivo (campo de texto)

Seleccionar archivo (input file)

Cada archivo cargado debe quedar listado como una entrada editable.

El botón debe funcionar como en “Agregar Maquinaria” de la bitácora: genera un nuevo bloque editable.

2. Al hacer “Guardar” el movimiento:
Todos los archivos listados deben subirse al bucket movement-files (uno por uno).

Cada archivo debe tener un file_path único (usando UUID).

Luego de subir a Supabase Storage, se debe insertar una fila en la tabla movement_files con:

Campo	Valor
file_path	Igual al path del archivo en Storage
file_name	Nombre escrito por el usuario
file_url	URL pública generada con getPublicUrl()
file_type	MIME type (image/jpeg, application/pdf, etc.)
movement_id	ID del movimiento recién creado
user_id	ID del usuario actual (desde contexto)
organization_id	Organización activa del usuario
visibility	"organization" (por defecto)

Detalles técnicos
Bucket de destino: movement-files

Tabla relacionada: movement_files

Ya están creadas las políticas RLS necesarias para que los archivos se suban y accedan correctamente.

Se puede reutilizar la lógica exacta que ya está funcionando en site_log_files.

Visualización esperada
Mostrar los archivos en forma de bloques listados debajo del acordeón “Archivos”.

Cada bloque debe incluir:

Nombre editable

Botón para subir o reemplazar archivo

Opción para eliminar ese archivo antes de guardar

Resultado esperado
Los usuarios pueden adjuntar facturas, tickets, imágenes u otros archivos a un movimiento.

El sistema sube correctamente los archivos al bucket movement-files y registra la metadata en movement_files.

Todo funciona igual que en el sistema de bitácora, manteniendo la misma estética, comportamiento e integración.