Necesito agregar mejoras avanzadas al Gantt que ya tenemos, como en Jira. Las mejoras deben aplicarse a todos los componentes que ya están en `src/components/ui-custom/gantt`.

🎯 OBJETIVOS

1. ✅ Scroll horizontal:
   - El Gantt debe tener scroll horizontal cuando hay muchos días.
   - Para esto, la sección de la grilla de días debe estar en un `div` con `overflow-x-auto` y `min-w-fit`.
   - Asegúrate de que las filas (tanto en `GanttGrid` como en `GanttRow`) estén sincronizadas y se desplacen juntas.
   - El scroll debe estar en la sección de timeline, no en toda la página.

2. ✅ Marcar el día de HOY:
   - Dentro de `GanttGrid`, agrega una línea vertical azul (2px) que marque el día actual.
   - La posición debe calcularse en base al día del mes actual y el ancho de día (ej. 40px).
   - Usa `position: absolute` para colocarla dentro del contenedor de la grilla.

3. ✅ Botones para cambiar la vista:
   - Crea un pequeño switcher arriba a la derecha del Gantt (puede estar en `Gantt.tsx`).
   - Botones: `Días`, `Semanas`, `Meses`.
   - Cambiar la vista cambia el ancho de cada columna:
     - Días: 40px
     - Semanas: 100px
     - Meses: 160px
   - Usa estado global (Zustand o `useState`) para guardar la vista activa.
   - Reutiliza ese valor en `GanttGrid` y en el cálculo de las barras (`GanttBar`) para ajustar `left` y `width`.

4. ✅ Drag & Resize de barras:
   - En `GanttBar.tsx`, implementá dos `handle` invisibles a los costados (left y right).
   - Usa `react-draggable`, `react-resizable` o lógica manual con `onMouseDown` + `onMouseMove` para permitir modificar:
     - El `startDate` al mover el borde izquierdo.
     - El `endDate` al mover el borde derecho.
   - Al soltar (`onMouseUp`), actualizá los valores de fecha.
   - Mantené los estilos coherentes con el resto del Gantt.

5. ✅ Estilo general:
   - El sistema debe verse profesional, tipo Jira.
   - Usa colores suaves, texto comprimido y barras bien alineadas.
   - Mantené los títulos alineados a la izquierda con indentación según `level`.

📁 ARCHIVOS INVOLUCRADOS:
- `Gantt.tsx`: debe contener el switch de vista y el wrapper con scroll horizontal.
- `GanttGrid.tsx`: renderiza los días o semanas o meses según la vista.
- `GanttRow.tsx`: sin cambios por ahora salvo ajuste de ancho.
- `GanttBar.tsx`: implementar los handles de resize + lógica de cálculo de ancho.
- `store.ts`: si usás Zustand para manejar la vista actual (`day`, `week`, `month`).

📅 FUNCIONES ÚTILES QUE PODÉS DEFINIR
```ts
const getDayOffset = (startDate: string) => {
  // Devuelve la cantidad de días desde el inicio del mes actual
};
const getBarPosition = (startDate: string, endDate: string, mode: 'day' | 'week' | 'month') => {
  // Devuelve { left, width } en px según la vista actual
};