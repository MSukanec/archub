Quiero que actualices el formulario de “Nueva Entrada de Bitácora” para que funcione correctamente y sea consistente con la base de datos.

### 🧠 Lógica general:
1. El formulario guarda una entrada en la tabla `site_logs` de Supabase.
2. Los campos `entry_type` y `weather_enum` ahora son enums en Supabase, con valores restringidos. NO deben ser campos libres.
3. El botón de "Guardar" actualmente no hace nada: arreglá el submit para que funcione correctamente.

---

### 🧩 Campos del formulario:

1. **Fecha del log:** 
   - Tipo: `date`
   - Campo: `log_date`

2. **Creador:**
   - Campo: `created_by`
   - Ya viene con el usuario activo, no editable.

3. **Tipo de entrada (entry_type):**
   - Enum (de Supabase): `avance_de_obra`, `incidente`, etc.
   - Mostralo como un `select`.
   - Mostrá icono + nombre legible (ej: 📝 Avance de obra)
   - Guardá el valor exacto del enum (snake_case).
   - Campo: `entry_type`

4. **Clima (weather_enum):**
   - Enum (de Supabase). Valores permitidos:
     - `sunny`: ☀️ Soleado
     - `partly_cloudy`: 🌤️ Parcialmente nublado
     - `cloudy`: ☁️ Nublado
     - `rainy`: 🌧️ Lluvioso
     - `stormy`: ⛈️ Tormenta
     - `snowy`: ❄️ Nieve
     - `foggy`: 🌫️ Niebla
     - `windy`: 💨 Viento
   - Mostralo como `select` con icono y texto legible.
   - Campo: `weather_enum`

5. **Comentarios:** 
   - Campo: `comments`
   - Tipo: textarea

6. **Entrada pública:** 
   - Campo: `is_public`
   - Tipo: booleano (checkbox)

7. **Marcar como favorito:**
   - Campo: `is_favorite`
   - Tipo: booleano (checkbox)

---

### 💡 Extras:
- El botón "Guardar" debe enviar el formulario a Supabase y mostrar feedback (éxito o error).
- Si el submit falla, mostrá el error en consola y un mensaje al usuario.
- Asegurate de usar valores del enum reales y no texto libre.
- Si es posible, reutilizá el estilo base de la app (input, select, etc.).

---

### 💻 Supabase:
La tabla se llama `site_logs`, y todos los campos mencionados ya existen.