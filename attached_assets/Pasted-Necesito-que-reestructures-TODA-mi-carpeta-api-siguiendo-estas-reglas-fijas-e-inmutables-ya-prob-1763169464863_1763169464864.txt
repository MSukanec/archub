Necesito que reestructures TODA mi carpeta /api siguiendo estas reglas fijas e inmutables, 
ya probadas para Vercel. No cambies nada más fuera de lo pedido.

REGLA 1 — NUNCA usar carpetas anidadas dinámicas como /api/a/[id]/b/
REGLA 2 — NUNCA usar index.ts dentro de carpetas.
REGLA 3 — CADA endpoint debe ser un archivo .ts en una carpeta que SOLO contenga parámetros estáticos.
REGLA 4 — Las rutas dinámicas SIEMPRE deben ser archivos individuales llamados [algo].ts dentro de una carpeta.
REGLA 5 — NUNCA debe haber dos archivos en la misma carpeta con nombres dinámicos distintos. Ejemplo: NO permitir [clientId].ts y [projectId].ts juntos.
REGLA 6 — Cada endpoint debe terminar SIEMPRE con: 
   return res.status(200).json({ ... })
Nunca retornar HTML, string o nada vacío.

ESTRUCTURA FINAL EXACTA (recreala y garantí que quede ası́):

/api
  /project-client
      [clientId].ts          // GET/PATCH/DELETE un cliente
  /project-clients
      [projectId].ts         // GET lista + POST crear cliente
  /project-clients-payments
      [projectId].ts         // GET pagos del proyecto
  /project-clients-summary
      [projectId].ts         // GET resumen del proyecto

REGLA 7 — Garantizar que estos archivos existen y son los ÚNICOS:
  /api/project-client/[clientId].ts
  /api/project-clients/[projectId].ts
  /api/project-clients-payments/[projectId].ts
  /api/project-clients-summary/[projectId].ts

REGLA 8 — Borrar cualquier archivo sobrante:
  api/project-client/[projectId].ts      // eliminar
  api/project-client/index.ts            // eliminar
  api/project-clients.ts                 // eliminar
  Cualquier otro endpoint duplicado o index.ts.

REGLA 9 — Verificá TODOS mis endpoint handler para:
  - importar correctamente createClient desde "@supabase/supabase-js"
  - leer process.env.SUPABASE_URL y process.env.SUPABASE_ANON_KEY
  - que SIEMPRE respondan JSON válido.
  - que jamás devuelvan texto plano ni HTML.

REGLA 10 — Revisá todos mis handlers relacionados (budgets, courses, community, etc.)
y confirmá que SIGUEN el mismo patrón sin romper esta estructura.

AL FINAL:
1) Mostrame la lista de archivos finales que dejaste en /api
2) Confirmame que Vercel NO tendrá rutas en conflicto
3) Confirmame que ningún endpoint devolverá HTML en vez de JSON
4) Confirmame que ya no existen rutas duplicadas, anidadas ni ambiguas