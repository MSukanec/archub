Quiero que crees un nuevo componente reutilizable llamado `ActionBarDesktop.tsx` y lo guardes en la ruta:

`src/components/layout/desktop/ActionBarDesktop.tsx`

Este componente será una **barra de acciones fija y reutilizable** que se ubica **por encima del contenido principal de cada página** en modo escritorio (desktop). Su objetivo es unificar botones como filtros, búsqueda, switches, acciones, exportar, etc.

### 🎯 Para esta primera implementación de prueba, hacé lo siguiente:

1. **Usá la página `Listado de Tareas` como ejemplo de implementación.**
   Ruta: `/construction/tasks`

2. Insertá el componente `ActionBarDesktop` por encima del contenido actual de la tabla.

3. Dentro de la ActionBar, agregá los siguientes elementos como prueba:
   - 🔍 Un **input de búsqueda** (`placeholder: Buscar tareas...`)
   - 🧩 Un **botón de Filtros** (icono: filtro)
   - ➕ Un **botón primario** con el texto: `+ Nueva Tarea`
     - Este botón debe ejecutar la misma función que actualmente se ejecuta cuando se hace clic en el botón de creación de tareas en esta página.

4. El componente `ActionBarDesktop` debe aceptar props que permitan inyectar distintos contenidos según la página. Por ejemplo:

```tsx
<ActionBarDesktop
  title="Listado de Tareas"
  onSearch={(value) => console.log(value)}
  onFilterClick={() => console.log('Abrir filtros')}
  onPrimaryActionClick={() => openCreateTaskModal()}
  primaryActionLabel="+ Nueva Tarea"
/>
Estilizá el componente con Tailwind para que tenga:

Layout horizontal (flex, gap-2, items-center, justify-between)

Padding: px-4 py-2

Fondo: blanco o gris claro

Borde inferior (opcional) para separar visualmente del contenido

Responsive: que se oculte automáticamente en pantallas móviles (hidden md:flex)

Este componente será reutilizado en todas las páginas de la sección Obra, como Bitácora, Cronograma, Presupuestos, etc., por lo tanto mantené la estructura modular y flexible. Usá props para que se pueda personalizar fácilmente en cada vista.