Quiero que modifiques la página de contactos en `src/pages/contacts/contacts.tsx` siguiendo estas instrucciones:

---

🔁 FUNCIONALIDAD NUEVA: `linked_user_id`

1. Se ha agregado una nueva columna `linked_user_id` en la tabla `contacts`, que puede vincular un contacto con un usuario real registrado en la tabla `users`.

2. En el modal de creación y edición de contactos (`CreateContactModal.tsx` y `EditContactModal.tsx`), agregá un campo de **autocompletado** para buscar y seleccionar usuarios existentes en Archub.

   - Este campo debe consultar la tabla `users` (o un endpoint server si es necesario) por `full_name` o `email`.
   - Debe mostrar como resultado: nombre completo, email, avatar y nombre de organización.
   - Si el usuario selecciona un usuario, se guarda su `id` en `linked_user_id`.

3. Si el campo `linked_user_id` tiene un valor:
   - **No permitir editar los campos** `first_name`, `last_name`, `email`, `company_name`, etc. (deben aparecer deshabilitados).
   - Visualmente mostrar que el contacto está vinculado a un usuario real, por ejemplo con un badge tipo `✔ Usuario de Archub` o similar.

---

🧑‍💻 VISUAL EN LA TABLA DE CONTACTOS:

En la tabla de `contacts.tsx`:

1. Agregá una columna con el avatar y nombre del usuario real (si `linked_user_id` está presente).
2. En esa fila, en lugar de mostrar datos locales (nombre/email/etc.), usá los datos del usuario real (de la tabla `users`) si están vinculados.
3. Mostrar ícono/link a su perfil (si existe).

---

🎨 UX RECOMENDADA:

- En el modal, mostrá el buscador de usuario como algo opcional.  
  Texto de ayuda: `"Si este contacto ya es un usuario de Archub, vinculalo aquí para mantener sincronizados sus datos."`
- Si se elige un usuario, mostrar su información en un bloque visual con avatar, nombre, email, etc.
- Botón para "desvincular usuario" y volver a usar campos editables manuales.

---

📦 ARCHIVOS QUE DEBÉS MODIFICAR O USAR:

- `src/pages/contacts/contacts.tsx`
- `src/modals/contacts/CreateContactModal.tsx`
- `src/modals/contacts/EditContactModal.tsx`
- `lib/supabase/users.ts` (crear helper `searchUsersByNameOrEmail(query)` si no existe)
- Usar componente `AvatarWithName` (si existe) o crear uno reusable

---

⚠️ IMPORTANTE

No eliminar ningún campo actual. Solo integrar esta nueva funcionalidad de vinculación con usuarios y actualización condicional de la UI. El contacto debe seguir pudiendo existir sin estar vinculado a ningún usuario (`linked_user_id = null`).