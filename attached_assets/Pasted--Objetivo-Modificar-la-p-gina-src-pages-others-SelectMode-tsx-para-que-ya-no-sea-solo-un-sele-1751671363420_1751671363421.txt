## Objetivo

Modificar la página `src/pages/others/SelectMode.tsx` para que ya no sea solo un selector de modo, sino un formulario de **3 pasos** de tipo multistep onboarding.

---

## Flujo general

La nueva página debe funcionar como un **wizard paso a paso**, solo visible al primer ingreso si `onboarding_completed = false`, pero también reutilizable más adelante si se entra manualmente desde el perfil.

---

## Pasos del formulario

### 🟩 Paso 1: Datos básicos
- Campos:
  - Nombre (`first_name`) → guardar en `user_data`
  - Apellido (`last_name`) → guardar en `user_data`
  - Tema (`theme`) → guardar en `user_preferences`

### 🟨 Paso 2: ¿Cómo conociste Archub?
- Select con opciones de un ENUM llamado `discovery_source`
  - Valores: `'YouTube'`, `'Instagram'`, `'TikTok'`, `'Google'`, `'Recomendación'`, `'LinkedIn'`, `'Twitter/X'`, `'Otro'`
- Si elige `'Otro'`, mostrar un input adicional llamado `discovered_by_other_text` (guardar en `user_data` como texto)

### 🟦 Paso 3: Seleccionar modo de uso
- Mostrar las 4 cards como en la versión actual
  - `Profesional`, `Proveedor`, `Mano de Obra`, `Exploración`
- Cuando elige uno:
  - Guardar el valor en `user_preferences.last_user_type`
  - Marcar `onboarding_completed = true`
  - Redirigir a `/dashboard`

---

## Comportamiento general

- Debe mostrar un paso por vez con botones de `Siguiente`, `Volver` y `Finalizar`.
- El botón “Finalizar” debe:
  - Validar los pasos anteriores
  - Ejecutar todas las updates a Supabase
  - Redirigir al dashboard

---

## Guardado de datos

Usar `supabase.from(...).update().eq(...)` para guardar en:

- Tabla `user_data`: `first_name`, `last_name`, `discovered_by`, `discovered_by_other_text`
- Tabla `user_preferences`: `theme`, `last_user_type`, `onboarding_completed = true`

---

## Extras

- Usar Zustand o Context para guardar temporalmente los datos de los 3 pasos antes del envío final
- Mostrar estado activo del paso (por ejemplo: `Paso 2 de 3`)
- Visualmente consistente con el diseño actual de los cards (modos de uso)
- Al entrar desde perfil (cambio de modo), saltearse los pasos 1 y 2, y solo mostrar el paso 3. Esto debe controlarse con `user.preferences.onboarding_completed`

---

## Archivos afectados

Modificar directamente:

src/pages/others/SelectMode.tsx

yaml
Copiar
Editar

Crear si es necesario componentes auxiliares como:

/components/onboarding/Step1UserData.tsx
/components/onboarding/Step2Discovery.tsx
/components/onboarding/Step3SelectMode.tsx

yaml
Copiar
Editar

---

## Estados de uso

- Si el usuario **no completó el onboarding**, redirigir automáticamente a esta página desde el `App.tsx`
- Si ya completó onboarding y accede desde perfil, mostrar solo el paso 3