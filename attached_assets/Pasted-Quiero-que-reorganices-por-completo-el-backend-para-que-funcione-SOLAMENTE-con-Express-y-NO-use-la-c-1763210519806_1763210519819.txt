Quiero que reorganices por completo el backend para que funcione SOLAMENTE con Express y NO use la carpeta /api ni ningún endpoint serverless.

INSTRUCCIONES EXACTAS:

1) ELIMINAR /api ENTERA
- Borra por completo la carpeta /api
- NO DEJES NINGÚN RESTO, archivos sueltos ni subcarpetas.
- NO crees nada dentro de /api, la carpeta no debe existir más.

2) ESTRUCTURA DEFINITIVA DEL BACKEND (crear y mover archivos)
Crea esta estructura EXACTA dentro de /server:

server/
  index.ts
  routes/
    index.ts
    clients.ts
    personnel.ts
    projects.ts
    payments.ts
    billing.ts
    subscriptions.ts
    contacts.ts
    admin.ts
    ai.ts
    community.ts
    user.ts
  controllers/
    clients/
    payments/
    projects/
    billing/
    subscriptions/
    personnel/
    contacts/
    admin/
    community/
    user/
  lib/
    supabase.ts
    auth.ts
    cors.ts
    utils.ts
    validators.ts
    rate-limit.ts

3) MIGRAR HANDLERS DESDE /api
Antes de borrar /api, copiá cada lógica de los archivos API hacia:
- controllers/<modulo>/<handler>.ts
- ajustá imports
- NO dupliques nada
- NO cambies la lógica interna

4) ACTUALIZAR ROUTES
Cada archivo en /server/routes/<modulo>.ts debe:
- importar express.Router()
- importar la lógica desde /server/controllers/<modulo>/
- registrar GET/POST/PATCH/DELETE
- exportar el router

5) ACTUALIZAR server/routes/index.ts
Debe combinar todas las rutas así:

import { Router } from "express";
import clients from "./clients";
import personnel from "./personnel";
import projects from "./projects";
import payments from "./payments";
import billing from "./billing";
import subscriptions from "./subscriptions";
import contacts from "./contacts";
import admin from "./admin";
import ai from "./ai";
import community from "./community";
import user from "./user";

const router = Router();

router.use("/clients", clients);
router.use("/personnel", personnel);
router.use("/projects", projects);
router.use("/payments", payments);
router.use("/billing", billing);
router.use("/subscriptions", subscriptions);
router.use("/contacts", contacts);
router.use("/admin", admin);
router.use("/ai", ai);
router.use("/community", community);
router.use("/user", user);

export default router;

6) ACTUALIZAR server/index.ts
Debe:
- importar express
- aplicar cors
- parseo JSON
- importar rutas desde server/routes/index.ts
- iniciar el servidor en process.env.PORT o 3000

7) NO modificar nada del frontend.
El frontend debe seguir llamando URLs tipo:
/server/<modulo>/<endpoint>

8) Revisar imports rotos y ajustar rutas relativas.
No inventes funciones nuevas.
No modifiques ninguna firma ni lógica.
No cambies base de datos ni queries.
Solo MOVÉ, ORDENÁ y REAGRUPÁ.

9) Confirmá al final:
- estructura final
- archivos movidos
- qué rutas quedaron disponibles
- que todo compila sin errores