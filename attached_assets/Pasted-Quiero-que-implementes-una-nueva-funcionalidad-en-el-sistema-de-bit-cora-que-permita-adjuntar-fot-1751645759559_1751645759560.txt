Quiero que implementes una nueva funcionalidad en el sistema de bitácora, que permita **adjuntar fotos y videos** en cada entrada (site_log).

---

## 🧱 ESTRUCTURA BASE

Ya está creada la tabla y el bucket:

- Tabla: `site_log_files`
  - `id`: uuid
  - `site_log_id`: uuid
  - `file_url`: text
  - `file_type`: text (`image`, `video`, etc.)

- Bucket en Supabase: `site-log-files`

No hace falta crear nada nuevo, solo usar lo existente.

---

## ✅ OBJETIVO

Agregar al **modal de creación y edición de bitácora** un nuevo acordeón:

📁 Fotos y Videos

yaml
Copiar
Editar

Que permita:

- Subir uno o más archivos (drag & drop o explorador)
- Previsualizar imágenes en miniatura
- Eliminar archivos antes de guardar (en el modal)
- Al guardar la bitácora, subir los archivos al bucket `site-log-files`, crear los registros en la tabla `site_log_files` y vincularlos con el `site_log_id` correspondiente

---

## 📦 DETALLES TÉCNICOS

1. **Posición del nuevo acordeón**: debe ir en segundo lugar, después de “Información Básica” y antes de “Eventos”.
2. El componente de subida debe ser reutilizable y estar ubicado en:
src/components/ui-custom/FileUploader.tsx

markdown
Copiar
Editar
Props necesarias:
- `value: File[]`
- `onChange: (newFiles: File[]) => void`
- `accept: string[]` (default: `["image/*", "video/*"]`)
- `maxSizeMB` (opcional)
- `multiple: boolean` (default: true)
3. El uploader debe permitir:
- Previsualizar imágenes
- Mostrar tipo de archivo si es video
- Borrar archivos localmente antes de subir
4. En el botón "Guardar", los archivos deben:
- Subirse a Supabase Storage con la carpeta `site-log-files/{site_log_id}/`
- Guardar el URL y el tipo (`image` o `video`) en la tabla `site_log_files`

---

## ✨ UX/Visual

- Las miniaturas deben ser cuadradas, con icono de tipo si es video.
- El componente debe verse bien tanto en desktop como mobile.
- En modo edición debe mostrar los archivos ya subidos previamente (consultando la tabla `site_log_files` por `site_log_id`) con opción a eliminar.

---

## 📂 Escalabilidad

Este sistema debe diseñarse pensando en que será reutilizable para otros módulos (por ejemplo: movimientos, tareas, etc). Por eso:

- El `FileUploader` debe estar desacoplado del modal
- El upload y escritura a Supabase debe separarse en una función utilitaria
src/lib/storage/uploadSiteLogFiles.ts

yaml
Copiar
Editar

---

## 🎯 Objetivo Final

- [ ] Nuevo acordeón "Fotos y Videos"
- [ ] Subida de archivos múltiples con preview y eliminación local
- [ ] Upload a Supabase y escritura en tabla al guardar
- [ ] Visualización de archivos existentes al editar
- [ ] Código reutilizable y bien separado
