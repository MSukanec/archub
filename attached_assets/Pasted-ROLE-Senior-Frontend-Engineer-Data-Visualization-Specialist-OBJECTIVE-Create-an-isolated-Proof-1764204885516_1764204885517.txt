ROLE: Senior Frontend Engineer & Data Visualization Specialist.

OBJECTIVE: Create an isolated Proof of Concept (POC) for a futuristic financial dashboard interface. This will live on a new, hidden route and will NOT replace any existing pages yet.

THE VISION ("The Financial Neural Lattice"): We want to move away from tables and lists. The new interface represents the project's finances as a living organic network.

A central, pulsating node represents the project's total capital/health.

Orbiting nodes represent clients/units.

Their size, color, and proximity to the center indicates their financial status and urgency.

The interaction must feel organic, fluid, and immersive (Zoomable User Interface), not robotic or paginated.

TECHNICAL STACK & REQUIREMENTS:

Framework: Use the current Vite + React + TypeScript setup.

Styling: Tailwind CSS for containers and layout.

NEW LIBRARIES (Crucial):

Install react-force-graph-2d (for the organic node visualization and physics simulation).

Install framer-motion (for organic transitions and zooming effects).

Data: Use MOCK DATA defined locally in the file. Do not connect to Supabase/Zustand yet. We want to validate the visual experience first.

STEP-BY-STEP INSTRUCTIONS:

1. Install Dependencies: Add react-force-graph-2d and framer-motion to the project.

2. Create the Isolated Route: Create a new page component located at src/pages/lab/FinancialLatticePage.tsx. Add a route in the main router (e.g., App.tsx) so I can access it via /lab/financial-lattice.

3. Define Mock Data Structure (in FinancialLatticePage.tsx): Create an array of 50-100 "clients" with varying status:

id, name, unitNumber.

status: 'healthy' (green), 'warning' (yellow), 'critical' (red).

debtAmount: number (critical nodes have high debt).

lastInteraction: date.

4. Implement the Main Visualization (ForceGraph): Inside the page, render the ForceGraph2D component.

Central Node: Create one large, central node representing the "Project Core" (glowing, pulsating).

Client Nodes: Render the mock clients orbiting the center.

Physics:

Critical (red) nodes should be attracted closer to the center and repel each other slightly.

Healthy (green) nodes should float further in the periphery.

Styling: The nodes should not be ugly circles. Use canvas drawing within the library to make them look like glowing, organic cells. Red nodes should pulse.

5. Implement the "Organic Zoom" Interaction (Framer Motion): When a user clicks on a "Critical" (red) node:

The camera should fluidly zoom into that node (using Framer Motion to animate the transition).

The node should "expand" organically to reveal an overlay with the client's details (Name, exact debt, a "Contact" button).

Clicking outside should fluidly zoom back out to the macro view.

DELIVERABLE: I want to be able to navigate to /lab/financial-lattice and see a dark-mode, organic universe of client nodes floating around a center. I should be able to click a red node and experience a smooth zoom-in to see details.