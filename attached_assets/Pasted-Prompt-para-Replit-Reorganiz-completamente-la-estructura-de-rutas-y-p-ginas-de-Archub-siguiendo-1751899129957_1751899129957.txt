Prompt para Replit:

Reorganizá completamente la estructura de rutas y páginas de Archub siguiendo esta lógica:

Crea una Landing Page pública ubicada en src/pages/Landing.tsx que se renderice cuando se accede a /. Debe mostrar una presentación de Archub similar a la de SUPABASE, botones a "Iniciar sesión" y "Registrarse", y un CTA fuerte.

Crea las páginas de autenticación:

/login → src/pages/auth/Login.tsx

/register → src/pages/auth/Register.tsx

/forgot-password → src/pages/auth/ForgotPassword.tsx

Redireccioná correctamente las rutas según el estado de autenticación:

Si el usuario no está logueado y entra a una ruta protegida (/dashboard, /organization, etc.), debe ser redirigido a /login.

Si está logueado e intenta entrar a /login, /register, o /, redirigir automáticamente a /dashboard.

La app privada debe comenzar en /dashboard y luego usar rutas como:

/organization

/projects

/finances

Todos los archivos deben guardarse siguiendo la estructura:

bash
Copiar
Editar
src/pages/
├── Landing.tsx
├── dashboard/
│ └── DashboardHome.tsx
├── organization/
│ └── OrganizationHome.tsx
├── projects/
│ └── ProjectsHome.tsx
├── finances/
│ └── FinancesHome.tsx
└── auth/
├── Login.tsx
├── Register.tsx
└── ForgotPassword.tsx
Ajustá el enrutador principal (App.tsx o Router.tsx) para reflejar esta estructura y asegurate que todo funcione correctamente con Supabase Auth y Zustand.

🧩 Bonus: Protecciones y Redirecciones
Incluí lógica en el App.tsx para que:

Verifique si el usuario está autenticado.

Si no lo está, redirija a /login.

Si ya lo está y visita /, lo redirija a /dashboard.

Ademas:

Agregá una página 404 (src/pages/NotFound.tsx) que se renderice en cualquier ruta que no esté definida. Debe mostrar un mensaje amigable y un botón que redirija al inicio (/) si el usuario no está logueado, o al dashboard (/dashboard) si sí lo está.

En el enrutador principal, esta ruta debe definirse como:

tsx
Copiar
Editar
<Route path="*" element={<NotFound />} />