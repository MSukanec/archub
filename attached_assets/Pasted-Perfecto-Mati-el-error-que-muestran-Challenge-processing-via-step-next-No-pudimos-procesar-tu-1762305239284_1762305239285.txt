Perfecto Mati — el error que muestran (“Challenge processing via step next”, “No pudimos procesar tu pago”, etc.) apunta a que algo en la integración de Mercado Pago no está bien configurado. Te dejo una lista de chequeo y los pasos para rastrear y solucionar el problema, adaptados al contexto de Archub para que puedas ir al fondo rápido.

✅ Lista de chequeo inicial

Verificá que estés utilizando las credenciales correctas (modo sandbox vs producción). Si usás credenciales de prueba en producción o viceversa, Mercado Pago puede rechazar. 
Reddit
+2
Mercado Pago
+2

Verificá que los valores back_urls, auto_return, payment_method_id, etc estén configurados correctamente según el método de pago / flujo (tarjeta, billetera MP, etc.). 
Mercado Pago
+1

Revisá el mensaje completo del error en consola o respuesta de la API, muchas veces Mercado Pago devuelve un status + error_message que da la pista (por ejemplo, “The card has expired”, “authentication required”, “challenge required”).

Verificá que tu webhook (notificación) esté activo y funcionando si usás flujo que lo requiere (por ejemplo para pagos con demora).

Verificá los logs de la consola de Mercado Pago: en tu panel de desarrollador hay «Movimientos» o «Pagos» para ver qué pasó con la transacción (rechazada, en proceso, aprobada).

Verificá si el método de pago elegido requiere 3-D Secure / “challenge” adicional y el frontend / usuario lo está completando. Según la doc hay un flujo de “challenge” que puede detener el pago. 
Mercado Pago
+1

🕵️ Paso a paso para rastrear el error específico
a) Capturar datos del error

Desde la consola que mostraste, tomá el objeto de error completo (por ejemplo: {"currentStep":challenge", "flowType":"redirect", ...}). Copialo y guardalo.

b) Consultar en la API de Mercado Pago el estado del pago

Usá el endpoint de pagos para ver el detalle del pago con payment_id o preference_id. En la doc buscan en “Get payment status”. 
Mercado Pago

Esto te dirá si el pago está “processing”, “approved”, “rejected” o “in_process”.

c) Revisá el método de pago

Si el usuario está intentando con tarjeta, billetera, etc — verificá en tu preferencia si ese método está habilitado en tu cuenta para el país. Algunos países/metodos requieren configuración adicional.

d) Revisá back_urls / auto_return

A veces el pago se aprueba pero el usuario se queda en espera porque auto_return no está configurado correctamente o back_urls no apuntan a URL válidas. Esto puede causar que se muestre “No pudimos procesar tu pago” aunque haya sido aprobado. 
Stack Overflow

e) Revisión de “challenge” o 3-D Secure

Cuando aparece algo como “challenge processing via step next”, significa que se generó un 3-D Secure o paso adicional que el usuario no completó. Verificá en configuración de tu preferencia si estás permitiendo ese flujo, y que tu UI esté manejando correctamente redirecciones o steps.

🛠 Próximos pasos sugeridos para Archub

En tu backend (Replit o servidor) loguea todos los datos de la preferencia/pago antes de redirigir al usuario (ID, status inicial, método de pago, timestamps).

Agregá en tu UI un mensaje más claro cuando el estado sea “in_process” o “pending”: “Tu pago fue enviado, por favor completá el desafío / verificalo desde tu banco”.

Hacé un test completo con tarjeta de prueba (modo sandbox) para replicar exactamente qué ocurre: aprobada, rechazada o pendiente.

Una vez verificado que en sandbox todo funciona, probá en producción con un pago real pequeño para confirmar configuración.

Si encontrás un código de error específico o mensaje de Mercado Pago (por ejemplo: error = “authentication_required”, “card_declined”, etc) — compartí ese código aquí y lo vemos para solucionarlo específico.