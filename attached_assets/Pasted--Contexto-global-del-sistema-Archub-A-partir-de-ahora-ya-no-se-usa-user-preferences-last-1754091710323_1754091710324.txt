### 🧠 Contexto global del sistema Archub

A partir de ahora **ya no se usa `user_preferences.last_project_id`** como único campo global. En su lugar, se implementó una nueva tabla llamada `user_organization_preferences` para guardar el `last_project_id` **por organización**, de forma más precisa y escalable.

---

### ✅ Nuevas convenciones y lógica

1. **Tabla `user_organization_preferences`:**
   - Guarda el último proyecto usado por cada usuario dentro de cada organización.
   - Es la única fuente válida para saber qué proyecto usar al cambiar de organización.

2. **La función `archub_get_user()`** fue modificada para:
   - Devolver `preferences.last_project_id` desde `user_organization_preferences`, y no más desde `user_preferences`.

3. **La función `archub_new_user()` también fue actualizada** para:
   - Crear automáticamente una fila en `user_organization_preferences` cuando se registra un nuevo usuario y se crea su primera organización.

---

### 🚀 Qué debe hacer Replit en el frontend:

#### 📍Al iniciar sesión (o al llamar a `archub_get_user()`):

- El objeto `preferences.last_organization_id` sigue viniendo desde `user_preferences`
- El `preferences.last_project_id` ya viene correctamente desde la nueva tabla
- ⚠️ Por lo tanto, **no se debe confiar más en `user_preferences.last_project_id` como algo global**

#### 🧠 Cuando el usuario cambia de organización:

- Llamar a una función tipo `getLastProjectForUserInOrganization(userId, organizationId)`
  - Esta consulta debe buscar en la tabla `user_organization_preferences`
  - Si hay una fila, usar su `last_project_id`
  - Si no hay fila, se puede crear con `last_project_id = null` (opcional)

#### 📝 Cuando el usuario selecciona o cambia de proyecto:

- Actualizar la tabla `user_organization_preferences` haciendo un **UPSERT** con:
  - `user_id`
  - `organization_id`
  - `last_project_id`

#### Ejemplo de UPSERT SQL (para backend o Supabase RPC):

```sql
insert into user_organization_preferences (user_id, organization_id, last_project_id, created_at, updated_at)
values ('...', '...', '...', now(), now())
on conflict (user_id, organization_id)
do update set last_project_id = excluded.last_project_id, updated_at = now();
✅ Reglas clave para trabajar:
user_preferences sigue existiendo, pero ya no contiene last_project_id confiable.

Siempre que se requiera el proyecto actual, consultar user_organization_preferences con la organización activa.

Al cambiar organización, también cambiar el proyecto asociado.